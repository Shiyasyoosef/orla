const express=require("express"); const c=require("../controllers/brandController"); const {authMiddleware}=require("../middleware/authMiddleware"); const {requirePermissions}=require("../middleware/permissionMiddleware"); const {PERMISSIONS}=require("../config/permissions"); const router=express.Router(); router.use(authMiddleware); router.get("/",requirePermissions(PERMISSIONS.CATALOG_VIEW),c.listBrands); router.post("/",requirePermissions(PERMISSIONS.CATALOG_MANAGE),c.createBrand); router.put("/:id",requirePermissions(PERMISSIONS.CATALOG_MANAGE),c.updateBrand); router.delete("/:id",requirePermissions(PERMISSIONS.CATALOG_MANAGE),c.deleteBrand); module.exports=router;
