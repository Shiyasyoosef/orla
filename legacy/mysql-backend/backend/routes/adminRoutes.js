const express=require("express"); const {dashboardHandler,dashboardOverviewHandler,dashboardSearchHandler}=require("../controllers/adminController"); const {authMiddleware}=require("../middleware/authMiddleware"); const {requirePermissions}=require("../middleware\permissionMiddleware"); const {PERMISSIONS}=require("../config/permissions"); const router=express.Router(); router.use(authMiddleware); router.get("/dashboard",requirePermissions(PERMISSIONS.DASHBOARD_VIEW),dashboardHandler); router.get("/dashboard/overview",requirePermissions(PERMISSIONS.DASHBOARD_VIEW),dashboardOverviewHandler); router.get("/dashboard/search",requirePermissions(PERMISSIONS.DASHBOARD_VIEW),dashboardSearchHandler); module.exports=router;
