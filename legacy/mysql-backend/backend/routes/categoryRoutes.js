const express=require("express"); const c=require("../controllers/categoryController"); const {authMiddleware}=require("../middleware/authMiddleware"); const {requirePermissions}=require("../middleware/permissionMiddleware"); const {PERMISSIONS}=require("../config/permissions"); const router=express.Router(); router.use(authMiddleware); router.get("/",requirePermissions(PERMISSIONS.CATALOG_VIEW),c.listCategories); router.post("/",requirePermissions(PERMISSIONS.CATALOG_MANAGE),c.createCategory); router.put("/:id",requirePermissions(PERMISSIONS.CATALOG_MANAGE),c.updateCategory); router.delete("/:id",requirePermissions(PERMISSIONS.CATALOG_MANAGE),c.deleteCategory); module.exports=router;
